#include <stdio.h>

int main() {
    float R, area;
    printf("Enter Radius (R) of circle: ");
    scanf("%f", &R);
    area = (22.0 / 7.0) * R * R;
    printf("Area of Circle = %.2f\n", area);
    return 0;
}
