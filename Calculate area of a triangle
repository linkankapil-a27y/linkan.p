#include <stdio.h>

int main() {
    float H, L, area;
    printf("Enter Height (H) and Base Length (L): ");
    scanf("%f %f", &H, &L);
    area = (H * L) / 2.0;
    printf("Area of Triangle = %.2f\n", area);
    return 0;
}
